# 更新日志

本文档记录 T-Translate 各版本变更。早于 v0.2.4 的历史请查 git log 与版本 tag。

## 未发布

### 听译

- **可以只听指定的程序了**：悬浮窗听译工具栏新增声音来源选择——默认全部声音，展开可只听某个程序。列表只显示已经发过声音的程序，正在响的带 ▶ 标记。做不到按网站区分（浏览器所有标签页在系统看来是同一个程序），Windows 10 无此接口只能听全部声音，选择器里会写明
- **音频捕获换成直接调用系统音频接口**：不再走浏览器的「共享屏幕」通道取声音——因此听译不会再请求屏幕捕获权限，也少了一次重采样，出字更快一点
- 换输出设备（插拔耳机）时由系统明确报出并自动重连，不再靠猜
- 模型加载失败时不再白重启一次，直接说明「文件可能不完整或不是识别模型」
- **系统音量开得再小也听得清**：「全部声音」模式此前抓到的是经过系统音量衰减后的信号，音量开到 8% 时识别器几乎收不到声音，只有改选指定程序才正常。现在按系统音量自动补偿回原始电平；静音仍然无声
- **音乐场景漏句更少**：识别到当前内容是音乐时自动放宽人声检测，唱段不再整行漏掉（一首歌的歌词行覆盖从 86% 到 100%）；说话内容不受影响
- **只听某程序时人声检测更稳**：改为向系统要原生 48k 立体声、自己降采样。系统自带的转换会让人声检测漏掉一部分句子（同一首歌同一播放器，86% 到 100%）
- **响着却长时间没字时自动放宽人声检测**：以前只提示「有声音但未识别到清晰语音」，现在提示的同时把检测放宽到音乐档，本次会话内不再收回
- **自动语言不再逐句乱跳**：连续三句判为同一语言后就钉住这个语言到会话结束，混播或带音乐的内容不会再冒出日文、粤语的乱码句
- 音乐里一两个字的碎句不再上屏、不再触发翻译
- 流式草稿的英文不再全大写
- 开始监听时状态先显示「正在连接音频」，连接超时从 10 秒缩到 3 秒并明确报错
- 只听的程序退出后自动切回全部声音并提示，而不是无声地听下去
- **轻声的来源也能识别了**：人声检测前加了一级自动增益（最多抬 30 dB，只抬不压，静音不动）。标准朗读集里极轻的录音此前整句进不了检测，英文整链词错率 22.9% 降到 14.7%
- **连续不停顿的讲话不再在词中间被切断**：9 秒硬切改为在最后 1.5 秒里最安静的那个瞬间下刀（多落在词与词之间），切点之后到下一段被识别之前的音频完整带进下一段，不丢也不重。此前一场 57 段的演讲里有 8 段开头是残词（"ut your"、"rote about"）
- 导出的字幕文件名带上时分（`listen-2026-09-02-1735.srt`），同一天导出两次不再互相覆盖；日期改用本地时间，晚上导出不再记到第二天
- **译文边出边显示**：定稿句的翻译改走流式，字一个个上屏，不再等整句翻完才出现；多句同时在译互不打断，字幕句也不再写进翻译缓存
- **翻译带上文**：翻每一句时把前两句一并交给模型当上下文（只作参考不翻译），人称、语气、指代不再句句重置。仅对 LLM 翻译源生效，谷歌、DeepL 等照旧
- 声音停下（视频暂停、没有程序在放）后电平条归零，不再停在最后一帧。系统环回在端点没有渲染流时根本不送包，此前电平条只能冻住
- **句子不再在逗号处被切碎**：人声检测的静音闭合从 0.35 秒放到 0.5 秒，句头预留从 0.32 秒加到 0.6 秒。整句解码比碎片解码准得多，英文整链词错率再从 14.7% 到 10.6%，中文 10.3% 到 10.1%；每句定稿晚 0.15 秒

### 朗读

- **字幕行可以朗读**：听译面板里鼠标停在某行右侧出现喇叭键，点它读这行译文（没译文读原文），再点停止。用「音频 → 读」里设的引擎和音色
- **朗读期间自动暂停收音**：任何窗口在朗读（系统语音、神经语音、外接服务都算）时，听译不再采集，停止后再放 300 毫秒尾巴，自己的声音不会被转写回字幕；顶部显示「朗读中 · 暂停收音」，电平条置灰。全平台可用，不依赖 Win11 的进程排除
- **设置页「听译模型」和「朗读设置」合并为「音频」**：进去先是「听」「读」两张状态卡，点卡进入。「读」里分「朗读 / 语音包」两页；「朗读」页是分段开关选引擎，一行「当前朗读」直接写明现在是本机哪个引擎还是 API 在读（外接服务带上次测试结果），音色按语言各一行，语速 / 音调 / 音量并排一行
- **音色改用选择面板**：点音色框弹出面板，可搜索、按女声 / 男声筛选，常用置顶，每个音色旁边一个试听键；系统语音同一面板，按语言分组
- 设置搜索命中「听译 / 识别」直接跳到「听」，命中「朗读 / 语音」跳到「读」
- **新增外接语音服务**：设置 → 文本朗读里填一个 OpenAI 兼容的 `/v1/audio/speech` 地址（本地跑的 IndexTTS、GPT-SoVITS、CosyVoice、kokoro-fastapi，或 OpenAI 本身），语音引擎里就多出「外接服务」。请求由主进程发出，走系统代理；密钥存在系统加密库里，离线模式下连解密都不允许，服务一律不可用。服务器没响应时这一句自动回落系统语音
- **新增神经语音**：设置 → 文本朗读里可下载语音包，装好后语音引擎多出「神经语音」。两个包可选：Kokoro（中文 100 个音色、英文 3 个女声，音质最好）和 MeloTTS（单一女声，中英夹杂的句子读得最顺）。合成全程在本机，不联网
- **按下即出声**：合成一句就播一句，不等整段合成完；长段落按钮按下后半秒内开始朗读。停止会真正打断合成，不是等它跑完
- **音色自动挑**：不选音色时按文本语言选常用音色；中英夹杂的句子在装了 MeloTTS 时自动改用它；语音包读不了的语言（如日文）这一句自动回落到系统语音，引擎设置不变
- 音色列表默认只列常用的几个，「全部音色」展开其余；系统语音仍是零下载保底，随时可选
- 语音包与识别模型同一份清单、各自目录（`models/tts-models`），卸载会先让引擎放开文件；离线模式同样不拉清单不下载
- 修复设置页在引擎已启动时改语速、音量或音色会报错（`updateConfig is not a function`）
- **神经语音按语言各定一个音色**：中文音色、英文音色分开选，各带试听；朗读时按文本语言取对应音色
- 指定了某语言的音色后一律用它，中英夹杂的句子不再被改派给 MeloTTS；没指定时才自动选
- 修复神经语音播放中再点朗读键不是停止而是从头重放（引擎在开始时先报了一次「空闲」，面板把目标丢了）
- 神经语音不支持调音调，音调滑块在该引擎下置灰并说明；语速、音量照常生效，作用于下一次朗读
- MeloTTS 读中文比 kokoro 快约两成（同一句 4.2 秒对 5.1 秒），按语音包配置的语速系数校正，语速滑块 1.0 在两个包上听感一致；英文不校正
- **英文里的数字改用英文读**：语音包自带的中文数字规则会把英文句子里的 `2026`、`$3.50` 改写成中文字，此前英文音色只能把数字念成中文。现在英文文本先把数字拼成英文单词（整数、小数、序数、百分比、货币、年份、时间、长数字串逐位）再交给引擎

### 安全与隐私

- **隐私模式页按模块说明**：模式选择改为分段开关；「当前模式功能说明」从四项扩成翻译源、OCR、听译、朗读、模型下载、历史与缓存、检查更新七项，每项写这个模式下的实际状态（可用 / 受限 / 不可用三色）；下面附一张三种模式的只读总览表
- **无痕模式逐模块梳理**：听译在无痕下开放，会话日志不写（会话中切到无痕也即时停写）；程序运行日志在无痕下只留错误一级，渲染进程的日志同样受限；Windows OCR 因为要把截图写成临时文件，无痕下不再参与识别（OCR 识别链与截图视觉两条路同一张允许表）
- 听译会话日志只滚动保留 3 份（原 20 份）：它只记指标与错误，够解释最近一次故障即可
- **离线模式下载入口复核**：新增 `npm run smoke:offline`，网络原语全换绊线后逐个调 OCR / 听译 / 语音包管理器、对应 IPC 与检查更新，全部以 `OFFLINE_BLOCKED` 拒绝、绊线零触发
- **修复模型包卸载可以删到目录之外**：精心构造的包名可让卸载删除模型目录之外的文件夹，OCR 与听译两侧都受影响
- **OCR 语言包补上离线闸门**：离线模式下不再拉取语言包清单、也不下载——此前只有听译侧有这道闸门；离线时设置页只列已安装的包
- 听译的进程间通道补上边界（音频块、事件、字幕导出各有上限）

## v0.4.0 — 2026-08-29 — 听译：实时字幕与逐句翻译

### 听译

- **悬浮窗新增听译模式**：实时转写电脑正在播放的声音，并逐句翻译成目标语言。工具栏的声波图标进入，点 ▶ 开始。识别全程在本机完成，音频只在内存中处理，不写入磁盘
- **支持中文、英语、日语、韩语、粤语**五种识别语言，可手动指定，也可交给自动判定（首句定档）
- **装上可选的草稿引擎后，中英会边说边出字**：不到一秒出首字、每 0.3 秒刷新一次，整句说完自动修正为定稿。不装则整句说完再出字，其余语言本来就走这条路
- **顶栏带一条实时电平**：跟着收到的声音跳动（约 0.1 秒一次，远快于出字）。声音在放却不出字幕时，一眼分清是「没收到声音」还是「收到了但没有可识别的语音」
- 字幕按句断行，长句自动切分，风格是歌词式滚动：正在识别的一行最醒目，已完成的淡出上移
- **可导出 SRT 字幕文件**，含原文与译文两行
- 无痕模式下听译不可用

### 模型下载

- **设置新增「听译模型」**：识别模型按需下载，不随安装包分发。基础模型 153MB（必需），草稿引擎 168MB（可选，只影响中英是否边说边出字）。没装模型时听译按钮灰显并提示去设置页
- **模型改存到程序安装目录**：OCR 与听译的模型现在都放在安装目录的 `models` 文件夹下，装在哪个盘就占哪个盘，不再持续撑大系统盘。安装目录不可写时自动回退到原来的用户目录；此前已下载的模型留在原处照常可用
- 离线模式下不下载模型（离线承诺绝不触网，这里不留例外）

### 隐私

- **听译的会话日志不再记录识别出的文字**，只留时长、耗时等调试指标——看视频不会在磁盘上留下字幕原文

### 修复

- **本地模型不认识的语言现在真的会让位了**：优先级靠前的本地模型若对目标语言没有把握（如 Llama 3 遇到长尾语言），会把该次翻译让给后面的谷歌等来源。此机制 v0.3.5 已加入，但一直只在关闭流式输出时生效，默认开启流式的主窗口从未真正用上

### 安全

- **设置存储通道加键白名单**：窗口页面经通用存储桥只能读写设置与引导状态；隐私模式只能通过带校验的专用通道切换，即使窗口页面被恶意内容攻破也无法改写隐私模式绕过离线限制
- 三个窗口恢复浏览器默认同源安全策略（网络请求 v0.3.1 起已全部由主进程代理，渲染层的历史豁免不再需要）
- 划词窗移除已无用途的密钥解密接口（该窗口的翻译早已走主进程，不应再能接触密钥）

## v0.3.8 — 2026-08-27 — 历史加密存储 + 卸载可清数据

### 隐私

- **翻译历史改为加密存储**：历史记录、收藏、统计数据整体加密后落盘（Windows 系统级加密，密钥绑定当前用户账户），不再以明文形式存放。升级后第一次启动自动完成转换，无需任何操作；转换成功后旧的明文数据即被清除
- 加密数据文件损坏时自动隔离并重新开始，不会卡住启动
- **数据总览新增两行**（设置 → 隐私 → 数据管理）：「历史存储」显示加密数据文件的体积；「文档进度」单独列出文档翻译续传数据的条数和体积，不再混在「界面数据」总量里。「清除全部数据」现在会一并删除加密数据文件

### 卸载

- **卸载时可选择同时删除全部用户数据**：卸载程序会询问是否一并删除设置、历史记录、已下载模型等全部数据，默认保留；静默卸载与自动更新不会触发该询问，也绝不删数据

### 修复

- 未配置翻译源时，翻译页顶部的提示条此前会撑开占据半个窗口，已恢复为一行细条
- 「风格改写」和「收藏」的首次引导气泡此前会挤成一列单字，已恢复正常宽度

## v0.3.7 — 2026-08-26 — 崩溃自愈 + 迁移包 + 右键菜单

### 右键菜单

- **资源管理器右键直达文档翻译**：PDF/DOCX/TXT 文件右键 →「用 T-Translate 翻译」，直接打开文档翻译页并载入该文件。程序没开会自动启动；已经开着（含收在托盘）会唤出窗口接手。卸载时右键菜单随安装程序自动清除，注册表不留痕迹

### 安全

- 加固恶意文档防护：修复加密文档密码框在文件名含特殊字符时的脚本注入风险；PDF 超大页面渲染、EPUB/DOCX 解压膨胀均加内存上限，异常文件不再拖垮程序

### 迁移

- **迁移包**：设置 → 隐私 → 迁移，一键把设置、术语库、收藏、自定义语言（含导入的 AI 动作）打包成一个 JSON 文件，到另一台电脑导入。导入时可勾选只要哪几样，重复条目自动跳过。API 密钥和模型文件不进迁移包——密钥换机后需重新填写

### 通知

- **长任务完成后发系统通知**：文档翻译、一键总结跑完时若窗口不在前台，从 Windows 通知中心提醒，点击回到窗口；窗口可见时仍只有应用内提示。设置 → 界面可关闭

### 历史

- **按来源筛选与配色**：历史工具栏下新增来源筛选，只看主界面、划词、截图或悬浮窗的记录（只有一类来源时不显示）；卡片按来源着浅色底——主界面淡蓝、划词淡紫、截图淡绿、悬浮窗淡橙，一眼分辨记录来自哪里。分组简化为按日期，去掉了按语言分组和分组选择器

### 朗读

- **拉丁字母语言不再一律用英语语音**：朗读语言为自动时，西班牙语、法语、德语等按文字特征挑选对应语音；挪威语等此前找不到语音包的语言也能正确匹配

### 修复

- **术语库导入的词条此前无法单条删除**（缺内部编号）。已修复，受影响的旧词条在下次启动时自动修复

### 稳定性

- **界面崩溃后自动恢复**：主窗口或悬浮窗的界面进程意外崩溃时自动重新加载，不再需要手动重启应用；短时间内反复崩溃则不再硬撑，重启进入安全模式
- **连续启动失败自动进入安全模式**：连续三次启动都没能正常运行时，下一次启动停用硬件加速等易出问题的环节并弹窗说明；恢复正常后下次启动自动退出安全模式

## v0.3.6 — 2026-08-25 — 术语检查 + 一键总结 + 理解结果入史

### OCR

- **新增泰米尔文、泰卢固文、卡纳达文三个语言包**，识别语言增至 59 种。这三种语言此前没有任何本地识别选项。⚠️ 它们的识别质量低于其他语言包（实测字符准确率约 71-74%，天城文包 87%），连写字符容易粘连、词间空格拿不到——够用来看懂大意，不适合需要逐字准确的场合
- 悬浮窗的反截屏设置此前从未生效过（窗口句柄没传对），现已修复

### 文档翻译

- **新增「术语检查」**：找出译文里没被翻译的术语，弹窗列出后一键替换。替换过的词在段落里高亮——原文的词和译文的词都会标出来，点高亮可以看到原词，也可以只撤销那一处
- **新增「一键总结」**：先逐段讲解全文，再把这些讲解汇总成总结。运行时可以随时停止，已完成的讲解会保留
- **讲解笔记和整理笔记随翻译进度一起保存**：关掉文档再打开（7 天内有效），恢复进度时讲解一起回来；只讲解没翻译的文档同样会保存。此前关掉文档，批量讲解花的模型调用就全白费了

### 历史

- **理解模式的结果现在会保存进历史**：悬浮窗理解模式的讲解作为独立条目保存（原文 + 讲解），卡片带「AI」徽章与「讲解」标签，可搜索、可收藏、可删除。同一段文字再次讲解会替换旧条目而不是堆一排；讲解不计入「翻译次数」统计；「恢复编辑」只回填原文，不会把讲解当译文塞进翻译框
- 悬浮窗的历史下拉里，理解条目也带「AI」小标

### 其他

- **「讲解」的图标从灯泡换成「AI」徽章**：文档段落的讲解按钮、讲解笔记标题、历史里的理解条目统一使用。灯泡保留给真正的提示（术语建议、首次引导）
- **划词翻译的字数上限从 500 提到 2000**，设置里可调到 5000（设置 → 划词翻译 → 字符数限制）
- **「总结」出现的门槛现在可以自己定**（设置 → AI 动作）。默认仍是 150 字；觉得该出现却没出现，调小它

### 引导

- 首次启动会有一个功能速览弹窗
- 翻译源没配好、或本地模型没启动时，翻译页顶部常驻一条提示并直达设置。配好的那一刻自动消失
- 改写风格和收藏各有一个一次性小提示

### 修复

- **总结的语言不跟随目标语言**：语言目录扩到 134 种后，其中 118 种到达模型时是个裸语言码，模型于是答英文；而缓存又没把语言算进去，导致换了目标语言仍然返回旧答案。三处一起修
- **术语库的译法按目标语言取**：同一个词若存了中文和法文两种译法，此前不分语言、谁排在前面用谁，可能把中文译法塞进法语译文
- 文档翻译页在特定情况下打开即崩溃
- 恢复翻译进度的提示此前显示的是内部文案键名，不是人话
- **无痕模式下，文档翻译进度和讲解笔记不再写入磁盘**（此前译文会随进度文件落盘）。无痕期间翻译过的文档，退出无痕后也不会补写，重新打开文件才恢复保存
- 界面报错、文档解析失败、视觉模型被自动停用时，日志会记下原因

## v0.3.5 — 2026-08-19 — 134 种翻译语言 + 56 种 OCR 识别语言 + 文档讲解

### 语言

- 可选语言从 16 种增加到 **134 种**，覆盖谷歌翻译支持的全部语言。每一个语言码都逐个向谷歌实际请求过一次翻译来核对，不是照着清单抄的
- **语言选择器换掉了原生下拉**：常用语言钉在最上面，下面按字母分组，顶部一条索引条可以点击跳转，也可以按住左右滑动快速翻找。索引跟着界面语言走——中文界面按拼音（荷兰语在 H），英文界面按英文名（Dutch 在 D）
- **可以自己添加语言**：选择器最下面「添加自定义语言」。填一个名字就行，另可单独指定"发给模型的名字"——本地模型可能只认「藏语」不认 Tibetan，也可能反过来。添加前会说明风险：这类语言不在谷歌翻译范围内，能否翻译取决于当前模型。添加过的在设置 → 翻译设置里可以删除
- **翻译冷门语言时更聪明了**：本地小模型碰到不认识的语言不会报错，而是流畅地胡说，程序原本会把这当成翻译成功、不再往下试。现在会先看模型认不认识这门语言，不认识就把谷歌翻译排到前面去
- 文档翻译的语言选择也换成了同一个选择器，与主界面共用最近使用和自定义语言

### 文档翻译

- **可以让 AI 讲解某一段**：段落右上角的灯泡，结果展开在译文下方，再点一下收起。适合翻完了还是看不懂的技术段落、公式、术语。讲解读的是原文，所以没翻译的段落也能用
- **攒够两段讲解后，可以「整理笔记」**：把已讲解的段落汇总成一份连贯笔记，按文档顺序排列。它**只覆盖你点开过的段落**，面板上会写明这一点——不是全文摘要

### OCR

- **识别语言从 12 种增加到 56 种**。缺口一直不在模型而在没放出来：内置模型本来就能读 37 种拉丁语言（此前只放了法德西），四个可下载语言包每个覆盖一整个文字族，此前每个只放了一种。现在西里尔包能选乌克兰/白俄罗斯/保加利亚/塞尔维亚/马其顿，天城文包能选马拉地/尼泊尔/梵语，阿拉伯包能选波斯/乌尔都/维吾尔——**不需要下载任何新东西**
- 语言选择器按模型包分组，哪些内置、哪些要下包，选之前就看得见
- 每种语言都拿模型字典核对过。**越南语和希腊语没有放出来**：它们看着像能读（越南语是拉丁字母），但模型认不出叠调符号，识别结果会把声调全部丢掉又读起来像正常文本——这种错比认不出更麻烦
- **识别不出来时会自动换引擎**。此前本地引擎遇到读不了的文字不会报错，而是返回空或者一串乱码，程序当它成功了，后面的 Windows OCR、在线引擎、视觉大模型一个都轮不到。现在会判断结果是不是真的读出了东西，没读出来就继续往下试

### 修复

- 检查更新弹窗里的状态图标贴在左边，与下方居中的文字不对齐（下载完成、安装中、更新失败三个状态都有）
- **进入历史页面直接崩溃**：上个版本修掉的"空译文毒化缓存"问题，在被修好之前已经把坏数据写进了历史记录（实测用户磁盘上有 55 条），缓存会自愈但历史不会，于是每次打开历史都必崩。现在启动时自动清理这类坏记录；收藏里的同类数据只清空字段、不删条目

### 其他

- 界面的报错现在会写进日志文件。此前界面崩溃、未捕获异常这些只停在开发者工具里，关掉程序就没了——上面那个历史崩溃就是因此在日志里完全查不到线索
- 日志里的报错不再显示为 `{}`（Error 对象被序列化成了空对象，等于每条报错的信息全丢）

## v0.3.4 — 2026-08-11 — 开箱可用性修复 + OCR 引擎补齐文字坐标

### 修复 · 全新安装

- **装完后进过一次设置，翻译就再也用不了了**：程序挑"默认启用哪个翻译源"时，取的是
  设置页列表里的第一个——而那一位是 OpenAI，十个源里唯一必须自己先去买 API Key 才能
  用的那个。所以全新安装的实际状态是：唯一启用的翻译源永远不可用。
  症状分两段，这也是它一直没被发现的原因：**没在设置页保存过任何东西时程序是能用的**
  （内部会自动降级到免费的谷歌翻译），但**只要从任意一个设置分页保存过任意一项**——
  改个主题、开个开关都算——这份"只启用了 OpenAI"的清单就落盘，之后每次翻译都报
  "没有可用的翻译源"，而提示完全指不到原因。
  现在全新安装默认启用 **LM Studio + Ollama + 谷歌翻译**，本地模型排在前面：装了本地
  模型的走本地、文字不出这台电脑；没装的会在瞬间跳过（本地连接是立刻失败，不会卡住）
  由谷歌翻译接手，不配置任何东西也能直接翻。**已有配置不受影响**，这只改全新安装

### OCR · 文字坐标

- **换个 OCR 引擎，悬浮窗的散点排版就没了**：散点模式（每块文字在原位贴译文，看漫画、
  界面标签、生词表时用的那个）需要引擎告诉程序每块文字在哪。此前七个引擎里**只有本地
  引擎会给坐标**，其余的都把位置信息丢在半路，于是一换引擎散点就静默退回整段。
  现在 **Windows OCR / Azure / Google Vision / OCR.space / 百度** 五个引擎全部补齐——
  其中三个的坐标本来就在返回结果里，只是没被读；OCR.space 是被主动关掉的开关；百度是
  用错了接口（`accurate_basic` 的 basic 就是"不含位置"的意思）。这五个引擎现在都能用
  散点模式了
- **百度 OCR 换接口后仍然兼容老账号**：取坐标要用的 `accurate` 接口在百度侧是单独开通、
  单独计费的。如果你的账号没开通它、或这个接口的额度用完了，会自动退回原来的
  `accurate_basic`——丢的只是散点排版，文字照常识别，不会让整次截图翻译失败
- **自动模式下引擎没给坐标时不再一声不吭**：以前只有手动选了"散点"才会提示"已按整段
  显示"，自动模式下默默走整段，看起来像是程序判断这段内容适合整段，其实是它根本没拿到
  判断依据。现在两种情况都会提示（LLM Vision 至今不返回坐标，属于已知限制）

### 修复

- **翻译后主界面白屏崩溃（偶发，与「风格」有关）**：某次翻译如果模型返回了空内容，
  这段空结果会被写进翻译缓存——而且写进去的是一个内部对象而不是文字。之后每次翻译
  同一段文字都会命中这条坏缓存，界面拿到对象直接崩成白屏。因为「风格」是缓存的一部分，
  换一个风格就能正常翻译，症状看起来像"日常风格坏了"。三处一起修：模型返回空内容现在
  按失败处理并自动换下一个翻译源；空结果不再写入缓存；读缓存只接受文字，读到坏数据
  就丢弃并重新翻译——**已经存进你磁盘的坏缓存会在下次翻译时自动修好**，不需要手动清理
- **主窗口位置每次启动都回到屏幕正中**：位置存进磁盘时是 `[x, y]` 数组，读回来却按 `{ x, y }`
  对象取，取到的永远是空——于是每次启动都被当成"从没保存过位置"重新居中，你把窗口摆在哪
  都留不住（宽高走的是另一条路径，一直正常）。已修；旧版本存下的数组格式照样能读回，升级
  后位置不会丢
- **日志文件名比里面的记录早一天**：文件名按 UTC 日期生成，文件里的时间戳却是本地时间——
  在 UTC 以西的时区（如北美）每天下午就切到"明天"的文件，一次晚上的使用会被劈进两个文件，
  按日期找日志也对不上。改为按本地日期命名

### 其他

- 导入的 AI 动作配置：`outputLanguage` 字段此前只校验"是个非空字符串"，写错的关键字
  会被当成字面语言名发给模型；现在按关键字（target/source/ui）或语言码校验

## v0.3.3 — 2026-07-29 — AI 动作框架：总结与理解

### AI 动作 · 新功能

翻译工具的定位不变，AI 动作是在你眼前这段内容上多加一层理解。一个动作 = 一份提示词配置（不是代码），主程序内置两个，其余靠导入。

- **新增「总结」**：长段内容读完给要点，三处都能用——划词卡、主面板译文栏、悬浮窗。结果在原处折叠展开（和「原文」共用同一块地方，同时只显示一个），再点一次收起。只对长段出现（中文约 150 字 / 英文约 120 词以上），零散的界面标签、单词不会冒出这个按钮
- **悬浮窗新增「理解模式」**（左上角开关）：打开后截图不再翻译，而是直接讲解内容在说什么——先说整体，再解释关键概念、术语、符号。它不卡长度也不看排版判定（打开开关本身就是触发），关掉即恢复正常翻译。开关是中性的框架：它跑哪个动作由配置决定
- **配了视觉模型就直接读截图**：不再经过「识别文字 → 再交给文本模型」，而是把截图本身交给视觉模型看——版面、图文混排都在，少两跳也少两处出错机会。模型其实看不见图（或端点没起）时自动退回文本路把事做完，不会白点一次
- **总结进历史，挂在翻译条目下**：不是独立的一条，而是某条翻译"还附带一个总结"。历史卡片右上角出现标记，双击详情能看到，可以单独删掉总结而保留翻译
- **可导入自定义 AI 动作**：设置页新增「AI 动作」，内置的两个列出不可改，另外可以导入 JSON 提示词配置（可移除）。字段不合规当场拒收并说明原因，提示词里只允许已知变量，配置外的字段一律丢弃
- **隐私跟随翻译**：无痕模式不写任何 AI 结果；离线模式下 AI 动作只走本机模型，视觉模型的端点必须在本机（截图不出本机）；只有传统翻译源（Google/DeepL 这类）时入口不出现——它们不具备对话能力，硬跑只会把提示词翻译一遍还给你
- **Anthropic Claude 与 Google Gemini 也能用 AI 动作了**：这两个源此前只实现了翻译一条路（它们的 API 本来就是对话接口，是我们没接），现在补上了对话调用——六个大模型源（OpenAI / DeepSeek / Ollama / LM Studio / Claude / Gemini）都可用 AI 动作
- **翻译源列表标出哪些没有 AI 动作**：只会翻译不能对话的源（DeepL / Google 翻译 / 微软翻译 / 百度翻译）在设置页的卡片上带一个标记，鼠标悬停说明为什么它当值时看不到「总结」和理解模式——省得以为是功能坏了

### OCR
- **LLM Vision 支持手动指定视觉模型**：OCR 设置的 LLM Vision 新增「模型名称」可选项——LM Studio / Ollama 同时加载多个模型时，此前不指定模型可能被选到不支持图片的模型（图片被静默丢弃、自动降级本地 OCR）；现在可填写视觉模型名（如 qwen2-vl-7b）精确锁定，留空保持原行为（用服务端当前加载的模型）

### 修复
- **悬浮窗透明度拉到极低时整段译文看不清**：v0.3.2 把透明度下限放开到 1% 后，整段模式的译文因为直接坐在近乎全透明的窗口背景上、失去了白底衬托而在漫画/桌面上糊成一片（散点模式因子窗格有独立背景不受影响）。现在给译文加了字幕式光晕描边——窗体可以隐身到 1%、文字始终在任意背景上清晰，正好是看漫画/视频时字幕悬浮的效果

## v0.3.2 — 2026-07-12 — 悬浮窗显示模式 + 顶栏柔化 + 下沉栈净删收尾

### 架构 · 旧渲染端栈净删（下沉专项批5 收尾）
- v0.3.1 翻译栈下沉后按计划隔一个回归周期（4 天累计日志零栈错误）拆除旧渲染端栈：services/translation.js（990 行）与 L2 缓存、providers 全树（翻译源运行时类 + 渲染端 OCR 五引擎类）净删约 4400 行——三窗翻译与 OCR 自此只有主进程一条代码路径
- provider 元数据收敛单源：设置页直接消费 stack 共享表 + 集中图标目录（显示顺序不变）；旧 OCR 识别 IPC 通道（ocr:paddle-ocr / ocr:windows-ocr）四端退役，识别统一走 stack
- localStorage 退役：旧 L2 缓存键启动清除（主进程缓存冷启动重建）；自定义免译过滤器数据一次性迁入主进程存储（无 UI 死 API 不再保留）；设置页「清除缓存」改为清真正的主进程缓存（此前清的是已废弃的旧键）

### 悬浮窗 · 显示模式与观感
- **散点/整段显示模式可手动指定**：截图翻译结果的排版此前全靠几何启发式自动判断，误判时整段文字被炸成一格格子窗、或散落的界面标签被并成一段。现在「悬浮窗口」设置新增「显示模式」：**自动**（按内容判断）/ **散点**（每块文字原位贴译文，适合界面标签、单词、漫画）/ **整段**（合并为一段译文，适合文章段落）；切换后用上次截图立即重排，无需重新截图
- **自动判定增强**：新增单词堆识别（生词表、单词卡不再被并成一整段）、稀疏文字识别（漫画气泡这类大图小字在自动模式下正确判为散点而非整段）、合并审计（热搜列表这类相邻行不再被粘连成一格）
- **散点子窗格去重叠**：界面密集截图的译文子窗格不再被"去重叠"逻辑推得偏离原文位置——只处理明显遮挡、就近微调、超出范围宁可保留轻微重叠也不乱跑
- **常驻穿透模式**：工具栏新增穿透开关，开启后内容区点击直接穿到下层应用（翻页、操作），顶栏保持可点、按 Esc 或再点退出；配合自动刷新，看漫画/追番时窗体不挡操作（按住 Alt 的瞬时穿透保留）
- **透明度可低至 1%**：透明度下限从 30% 放开到 1%，看漫画时窗体近乎隐身只留译文
- 修复：长段译文悬停时，复制图标不再挤占空间导致整段文字换行位移（改为覆盖在角落）

### 主题 · 顶栏柔化收尾
- **浅色/清新顶栏柔化**：延续 v0.3.1 深色顶栏的做法，浅色主题的亮蓝渐变顶栏改为冷调轻染表面、清新主题改为淡渐变；活跃页签统一为实心强调色胶囊，标题栏维持石板灰不变。设置界面的侧栏渐变与渐变标题字（清新主题）保留
- **文档翻译按钮改液态玻璃**：紫色硬编码渐变退役，改为跟随主题的磨砂玻璃质感——玻璃透出背景、强调色只落在文字上（浅色蓝 / 深色琥珀 / 清新翡翠），打开文档翻译时回到与其他页签一致的实心激活态

### 修复
- **划词/悬浮窗跟随目标语言，"已是目标语言"行为可配置**：此前选中内容已是目标语言时被硬编码翻转成中英互译（目标设英文却把英文翻成中文、目标日语时日文被翻成中文），且不可关闭。现在"翻译设置"新增「内容已是目标语言时」：**显示原文**（默认，直接展示原文、不调用翻译、不写历史）或**自动互译**（保留原行为）；划词窗与悬浮窗共用同一行为，悬浮窗原「锁定目标语言」开关随之退役（旧配置自动迁移）

## v0.3.1 — 2026-07-06 — 翻译栈下沉主进程 + 三主题柔化 + OCR 体验小批

### 协议
- **开源协议自本版起从 MIT 更换为「T-Translate 许可协议 1.0」**（源码开放，中文文本为准）：使用/修改/分发（含商用环境）保持全部免费；核心限制为——本软件及任何包含本项目代码的修改版（含修改者自行新增的功能）**禁止一切收费形式**（售卖/收费下载/内购/打赏解锁/收费分享），商业售卖须按 LICENSE 第九条另行洽谈授权；教程评测变现、有偿技术服务、不与功能挂钩的自愿捐赠不受限制
- 新增 NOTICE 文件（第三方依赖协议清单，全部为 MIT/ISC/BSD/Apache 系宽松协议）；作者署名 Edan Zeng；**v0.3.0 及更早版本仍按其发布时的 MIT 协议，不受影响**
- 双语 README 同步：零焦点截取功能与 `Ctrl+Alt+Space` 快捷键、「主进程单点强制」隐私宣称（下沉后的结构化事实）、项目结构与技术栈补 src/stack 与 esbuild、版本徽章与协议徽章更新

### 架构 · 翻译栈与在线 OCR 归一主进程（design doc 四阶段拍板后分六批落地）
- **三窗翻译归一单实例**：主窗/悬浮窗/划词窗此前各持一份翻译栈（各自缓存、各自故障计数、启动各解密一轮密钥）；现收敛为主进程单实例——同一句话任何窗口命中共享缓存不再重复调 API、翻译源故障一处计数全局绕开、设置保存一次生效一次（多窗广播 reload 机制退役）
- **隐私语义物理化**：渲染进程不再含任何翻译/在线 OCR 网络代码——离线模式"绝不触网"从"每个调用点自觉传参"（历史上漏过两轮共 4 处）升级为结构事实；privacyMode/useCache/OCR 引擎白名单参数从渲染端 API 面删除，主进程每请求按实时模式强制：无痕不读写任何缓存、离线走白名单 + requiresNetwork 双闸，截屏这一最敏感输入的引擎白名单由主进程注入、渲染端无法放宽
- **取消翻译真的取消**：清空/重译/关闭窗口即中断上游 HTTP 请求（请求 id→AbortController，含 sender 销毁联动与超时兜底）——此前被取代的请求在后台跑完，白耗 token 与本地 GPU
- **在线 OCR 下沉**：OCR.space/Google Vision/Azure/百度 + LLM Vision 五引擎随栈迁入主进程，密钥只在主进程解密（入统一审计）；LLM Vision 降级锁三窗全局生效（一窗撞坏模型锁定后其他窗口不再重复撞）、设置页复位一次全局复位；本地引擎改主进程内直调，识别少一跳 IPC
- **网络行为不变**：主进程栈走 Chromium 网络栈（net.fetch），系统代理与企业证书行为与迁移前完全一致
- 内部：esbuild 单脚本打包链（src/stack ESM 源 → 主进程 CJS 产物，dev/build/test 三链零接触）；密钥访问审计抽共享模块，主进程内解密与 IPC 解密同一份账本；新增栈单测 21 例（隐私过滤/降级链/缓存/abort/OCR 锁/批量解密审计）

### 悬浮窗 · 零焦点截取（实时字幕 / 追番场景）
- **新增全局快捷键「悬浮窗重新截译」（默认 Ctrl+Alt+Space）**：目标应用（会议软件/播放器/浏览器）保持前台、不失焦的前提下重截悬浮窗盯住的区域——此前窗口内 Space 触发需要悬浮窗抢焦点，Teams 等应用一失焦就隐藏字幕，截到的是字幕底下那层
- **新增自动刷新**：点工具栏刷新按钮选间隔（2/3/5/10 秒）即开始循环截译，适合听课/看视频挂着不管；内容不变的帧零开销零闪动（静默哈希去重），拖动窗口/手动截图/关闭窗口任一动作自动停止
- 说明：Teams「弹出字幕窗口」自带系统级反截屏（任何截图工具都截不到）——把字幕固定在会议窗口内即可正常截取

### 修复（下沉回归与实测发现）
- LLM Vision 对非视觉模型的 200"假成功"验真：服务端丢弃图片、文本模型硬答的响应（usage 图片 token 缺失）不再被当成识别结果，自动降级本地 OCR；"No models loaded"（LM Studio 空载）同样自动降级；降级提示随截屏→划词链可见（3 秒自动消失），锁定状态附带提示
- 划词窗"彻底不显示且零报错"根治：常驻窗口的渲染进程死亡后（渲染崩溃等）此前被永远复用为不可见僵尸，触发图标与结果卡全部投递给尸体；现检测渲染进程状态自动重建（含误伤修正：dev 热重载的 aborted 加载不算死亡）
- 截屏结果卡贴屏幕边缘展开不再跑出屏外（展开尺寸对工作区通用钳位）
- 翻译/OCR 管线日志落盘修复：栈内日志此前绑死 console 只进 stdout，日志文件对整条管线失明
- **全局快捷键不再可能录成"裸键"**：此前改键输入框按下任意单键立即保存且无任何校验，误按退格会把 Backspace 绑成系统级全局快捷键——只要程序在运行，全系统的退格键都被吞掉且毫无提示。现在改键必须包含 Ctrl/Alt/Win（F1-F24 功能键可单独使用），录制界面即时提示、主进程同规则把关；启动时自动把历史版本已存入的裸键绑定修复回默认值

### 主题（全面柔化，方案经四轮拍板）
- **浅色 · 石板蓝灰**：纯灰中性色注入冷石板色相、边框后退半档、文字对比从 17:1 回收到 14:1——告别"毛坯感"，蓝色强调不变
- **深色 · 琥珀夜读（护眼向）**：暖中性炭底座去蓝光、柔白文字降眩光、强调色换低发光琥珀金（金底深字）；**顶栏亮蓝渐变改暗表面**，强调色只标记活跃页签
- **清新 · 青碧水色**：底色从"泛青冷白"加饱和到湖水青碧，文字换同族墨青；翡翠强调、侧栏渐变、渐变标题字全部保留
- **划词窗主题跟随 + 可选彩虹签名**：划词窗默认严格跟随主题——浅色石板、深色暖炭琥珀、fresh 全新青碧翡翠配套皮肤（翡翠→青渐变激活钮与悬浮窗同语言）；新增「彩虹划词窗口」开关，开启后彩虹签名（原 fresh 专属粉彩升级为八色，前身"rainbow 主题"遗产，呼应彩虹树 Logo）接管全部主题的划词窗，卡片底面仍随主题
- **内部**：新增 `--text-on-accent`/`--floating-on-accent` 令牌并全仓迁移强调面白字（35 处）；111 处硬编码蓝 tint 迁 color-mix 令牌式写法；悬浮窗深色板岩残留全部暖化；主题文档修真（删已弃案的 --custom-accent 段落，补独立窗口令牌说明）

### 性能
- **OCR 设置页不再卡顿**：进入页面触发的自动健康检查此前在主进程同步加载原生模块（实测 ~115ms 冻结事件循环）并构建 ONNX 会话（~500ms，期间事件循环反复停顿、单次最长 56ms）——现默认只做轻量文件校验，深度检查（真实建会话验模型）留给手动"重新检查"按钮与修复下载后；本地引擎为当前选中引擎时，启动 5 秒后空闲预热原生模块，首次截图识别也随之提速

### 内部
- doc_cls 旋转分类器实测弃案：倒置整图可纠正，但日文横排截图被误判为竖排、韩文包输出乱码——主场景回归换边缘收益不可接受，结论与复现方法记录于 docs/OCR_MODELS.md
- 全仓 lint 首次归零：document-parser 5 处 case 内声明加块作用域、scripts/ 存量 8 处未用变量清理，no-case-declarations 恢复 error 防线
- SettingsPanel 死样式清扫净删约 800 行：旧折叠版 OCR UI、旧 TTS 布局簇、单选按钮簇、setting-card 族、拉丁输入簇及 themes.css 配套覆盖等 45+ 选择器（保留被收藏面板复用的 .tag-item；三主题 headless 实拍验证零破相）

## v0.3.0 — 2026-07-04 — 设置页 + Provider 管理专项 + 本地 OCR 升级 v6

四维审计（README 验真 / 设置桶漂移 / 性能健壮性 / 交互死代码 + 隐私对照）后分七批修复，含 1 个 P0、11 个 P1 与大批 P2/P3；另含 emoji→lucide 排版回归修复与三批技术债清扫（死通道/幽灵桶/死语言包键/CSS 令牌化，净删 ~700 行）。追加本地 OCR 基础模型换代 PP-OCRv6。

### 本地 OCR
- **基础模型升级 PP-OCRv6-small（单模型 50 语言）**：简中/繁中/英/日全保持且置信度更高，**新增内置法/德/西等 46 个拉丁语系**——Grüße/Straße/niño/¿ 等字符 v5 会错读（ß→B、ñ→n），v6 全对；识别速度同量级
- **拉丁语言包退役**：法/德/西开箱即用无需下载；已安装的拉丁包显示"已安装"并可正常卸载
- **新增"模型档位"**：标准（内置）/ 高精度（可选下载 PP-OCRv6 medium，~95MB）——高精度对模糊照片、艺术字、点阵字等困难样本更强；切换即时生效，切回标准不删包、再切回免重复下载
- **老版本兼容**：模型清单新旧双轨，v6 换代前的客户端仍取 v5 基础包与拉丁包，不受影响；引擎空格启发式改按模型代际门控（仅 v3/v4 开启）
- 基础包下载体积 18MB → 25MB；韩/西里尔/天城/阿拉伯语言包保留不变
- 语言包/模型清单手动刷新后给出明确反馈：有更新提示条数、已最新亦有提示（被动式，不主动打扰）

### 安全与隐私
- **在线 OCR 密钥不再明文落盘**（P0）：OCR.space / Google Vision / Azure / 百度 OCR 的 API Key 此前以明文写入 electron-store 磁盘 JSON（`encrypted` 标志无人消费），现统一走 Windows DPAPI 加密存储，存量明文一次性迁移；离线模式解密门同步覆盖
- **清空 API Key 真的清空**：Provider 密钥清空保存后此前会在重启时从加密存储"复活"并继续参与联网；现清空即删除密文条目；加密失败此前静默剥离 key 却报保存成功，现中止保存并报错
- **无痕模式补漏**：无痕下"清除历史"此前退出无痕即从暂存区复活（暂存区未清），已修；无痕下导入历史此前被退出时的暂存区恢复静默吞掉，现禁用导入并提示
- **离线模式补漏**：设置页"测试连接"与"检查更新/下载"此前绕过隐私门控直接联网，现分别在服务层与主进程拦截；悬浮窗散排翻译补缓存门控，无痕下不再把截图文本留在内存缓存
- 全部翻译源被禁用时此前静默回退默认优先级、仍调用被禁的云端源，现尊重"全部停用"；离线模式功能矩阵"本地统计不记录"与实现不符，已如实展示
- **密钥访问审计误报修复**：多窗口架构下启动加载 + 保存后广播重载会批量解密全部密钥，旧计数把这些自身流量算进警戒线——开程序后改一次设置就弹"检测到异常的密钥访问"假警报；现应用内批量载入（设置页加载/翻译栈重载/OCR 配置）只记审计日志不计入警报，未标记的零散爆发照常告警（新增单测锁死）

### 设置保存与重置
- **保存不再丢数据**：保存按钮此前按当前分页路由，跨页保存双向静默丢弃改动（翻译源页与其它页互相覆盖）；重构为按桶记脏 + 统一保存，任意分页保存都完整
- **重置补全**：重置所有设置此前只清 electron-store，漏掉主题 / 界面语言 / 侧栏偏好 / zustand 偏好 / 开机自启等边带存储，现逐一清理且不再把旧运行时态搞乱（OCR 区、划词开关反相）；API 密钥保留并在确认弹窗明示
- **"未保存"不再说谎**：自启划词 / 语言 / 主题 / TTS 等即存即用的控件不再误标未保存；快捷键改绑失败不再留脏态；删除 Electron 下从不生效、还会静默吞掉菜单重载的假 beforeunload 保护
- 界面/启动设置桶补进默认表，重置或全新安装打开"界面"页不再崩溃

### Provider 与多窗口
- **多窗口即时生效**：Provider 配置保存后，悬浮窗 / 划词窗此前需重启才生效，现保存即广播两窗热重载；三窗共享的翻译缓存改为按时间戳合并，不再互相覆盖
- **LLM Vision OCR 端点可配**：此前被全链消费却无 UI 入口（硬编码 localhost:1234），现 OCR 面板 LLM Vision 组新增端点输入框，旧 connection 桶删除并迁移
- **Provider 修复**：Ollama"留空自动检测模型"从空谈变真实；Anthropic 流式改惰性空闲超时（原 30 秒总超时会砍断健康长流）并校验截断不当成功缓存；DeepL 不支持的语言不再直送 400 拖垮整个 DeepL；百度长文本改 POST（原 GET 超 URL 上限失败）；OCR.space 语言码正确映射；google.cn 已停服选项删除；流式翻译失败不再误报"没有可用的翻译源"
- **划词/OCR 引擎单一真相源**：OCR 引擎偏好此前 zustand 与 electron-store 双持久化会分叉，现以 electron-store 为准启动播种

### TTS
- 状态回调从单槽改多监听：访问过设置-语音页后主面板朗读状态图标此前永久失灵，已修；朗读前惰性重读设置，改设置后无需重启即生效；主面板朗读按钮随 TTS 开关即时显隐

### i18n 与文档
- Provider 层用户可见错误文案全量迁 i18n：英文界面不再看到中文报错；错误分类器补中英关键词并加双语单测锁死
- 划词窗翻译计入主窗口历史（此前写入无人读取的内存列表）
- README 修正：隐私模式四档→三档（严格模式已删）、快捷键表更正、OCR 引擎 6→7、技术栈 Electron 42 / Vite 7

### 内部
- 死代码清扫约 500 行：主进程四个死在线 OCR handler、SUBTITLE 幽灵通道桶、多处死 IPC 监听与导出、creative 模板与死元数据、theme 'auto' 死分支等；About/OCR/快捷键面板 emoji 图标改 lucide
- 深色主题下原生下拉弹层白底白字修复：全局 `select option` 令牌化，设置页/主面板全部下拉受益（三主题实测计算样式）

## v0.2.9 — 2026-07-02

### 主界面翻译面板（全面体检修复 29 项）
- **数据安全**：无痕模式中退出应用会用空历史覆盖真实数据——历史+统计一次性永久丢失（暂存区未持久化），已修；无痕模式切换此前从未真正同步到主进程（跨进程常量表漏了 secure，setMode 静默被拒）
- **隐私**：主面板全部 5 个 OCR 入口（截图/拖拽/导入/粘贴/静默截图）与悬浮窗截图不感知隐私模式引擎白名单——离线模式下配过 key 的截图可直发在线 OCR 服务，已统一接入门控；悬浮窗 provider 解密现在遵守离线模式的在线 key 门控（原直连 safeStorage 绕过）
- **严格模式删除**：定义与执行长期脱节（历史照存、缓存照写、功能禁用矩阵无执行点），v0.2.4 即已决定放弃；持久化值双端自动迁移到离线模式
- **设置不再重启即忘**：自动翻译/流式输出/翻译延迟三开关此前从不持久化（保存写入的是无人读取的安慰剂键）；在线 OCR 的 API key 此前每次重启后丢失、直到重新保存一次设置；LLM Vision 端点此前硬编码 localhost:1234 不跟随连接设置
- **悬浮窗 provider 配置修复**：读取的是迁移前的 legacy 桶（新装用户拿到零 provider、升级用户用冻结旧配置），空配置还会截断回退链；设置保存后悬浮窗常驻期间现可热刷新 OCR 配置
- **语言同步修复**：store→electron-store 同步层因缺中间件整体静默失效多版本（从历史恢复的语言不进划词翻译）；散落直写收敛为单一同步点；设置页保存不再把加载时快照倒灌回语言键
- **交互修复**：流式翻译中 Ctrl+Enter/切换风格模板不再能开出第二条交错写入的流；流中清空后译文不再"还魂"；术语替换提示不再一次性消失（首次 5 秒后永不再现）；"不再提示此术语"从空头支票变真持久化（跟随收藏导出导入）；TTS 朗读修正传参字段（法语/德语等拉丁语系译文此前恒被英文语音朗读）；慢速 LLM 下重开收藏弹窗不再被迟到的旧分析覆盖标签摘要；长通知不再被先前短通知的计时器提前掐灭；截图自动翻译不再误报"请输入文本"、不再丢 OCR 模板与划词气泡回显
- **风格改写提示词双语化**：英文界面不再拿到中文改写指令（弱模型输出语言污染）
- 内部：App.jsx 包裹全部 hooks 的 try/catch 拆除（9 处 rules-of-hooks 违规根修，eslint 豁免删除恢复 error 防线）；启用 react/jsx-uses-vars 消除约 190 条 lint 误报；死代码清尾（三个死组件 ~150 行、死截图管线、死 IPC 健康检查全链、僵尸统计字段、privacy.css 整文件、settings 平铺死键簇十余个、设置导入导出死函数）；设置页 window.confirm×6 全部替换为主题化 ConfirmDialog

### 历史记录 + 收藏（体检修复 17 项）
- **安全**：AI 标签 / 保存摘要 / 风格改写三个 AI 功能此前不感知隐私模式（离线/无痕下内容流向在线源）；新增隐私选项单一出口并全部接入
- **修复**：访问过历史页后主翻译输入框方向键失灵（隐形键盘导航无守卫地全局拦截按键，已删除）；历史/收藏页 Ctrl+F 此前聚焦一个从未渲染的输入框，现在真正聚焦面板内搜索框；统计面板的语言对区块样式接错旧标记，语言条完全不可见，已复活；英文界面月份分组显示中文（dayjs 全局锁死 zh-cn）；连接失败提示显示裸键名（i18n notify 重复定义互吞）
- **隐私页"自动删除 N 天前历史"从摆设变真实现**（启动时清理）；删除四个持久化多版本但无人消费的幽灵设置键
- **历史导入**：字段校验与规范化（缺 id 补全、坏时间戳兜底，不再出现 Invalid Date 分组）、按内容去重、提示真实新增数；导出加版本包装（导入兼容旧格式）
- **划词/悬浮窗的翻译现在计入状态栏"今日"计数**
- 内部：新增共享组件 HighlightText（两面板原各带一份拷贝）、ConfirmDialog（替换四处阻塞式 window.confirm）、useVisibleHotkey（可见性守卫的全局快捷键，三面板共用）、AI 提示词共享工具（收藏的中文版重复提示词删除，跟随界面语言）；eslint 重复键豁免删除恢复 error 级防线；历史/收藏死代码清尾；新增导入规范化单测 5 例

### 文档翻译（全面体检修复 28 项）
- **安全**：文档翻译此前不感知隐私模式——离线/无痕模式下照走在线翻译源、无痕模式译文照写磁盘缓存；现已全调用点接入隐私门控，扫描件 OCR 同样遵守隐私模式引擎白名单
- **设置页 9 个控件从摆设变真实生效**：settings.document 桶此前三方漂移（默认值一套键、设置 UI 写另一套、组件运行时全部硬编码），现统一为分段长度 / 并发数 / 显示样式 / 智能过滤，旧值自动迁移
- **"批量模式"换真并发**：原实现为逐条串行循环（零加速），且批内部分失败会把空译文标成已完成；改为 worker 池逐段并发（默认 2、设置可调 1-6），每段独立成败可重试
- **术语库联动复活**：开关自始无效（option 键名拼错），现单段/并发/重试/重译全路径生效
- **进度恢复兑现承诺**：原先只在翻译自然结束时保存，中途崩溃/退出全丢；现运行中节流落盘 + 退出时同步兜底 + 编辑译文即保存，过期进度自动清扫
- **扫描 PDF 支持 OCR**：原 OCR 接线为死代码（扫描件静默显示 0 段）；现逐页检测，图像页渲染后走 OCR 引擎链，混排 PDF 按页混合提取，解析中显示 OCR 页进度；全无文字时明确提示
- **导出修缮**：导出走真实保存对话框（原调用不存在的 API，永远落入浏览器下载兜底）；SRT⇄VTT 交叉导出转换时间码格式（原产物会被播放器拒载）；Word/PDF 导出保留段内换行；导出重新顺序编号
- **解析健壮性**：GBK/UTF-16 编码自动探测（存量中文字幕/小说不再乱码）；EPUB 兼容任意 manifest 属性顺序（原先部分生成器的书整本漏配）；CSV 引号内逗号不再切错列；SRT 脏序号（重复/重启）不再撞段 id；超 20MB 文件明确拒绝（原守卫是死代码，超大文件会冻结界面）
- **交互**：搜索高亮不再隔个漏标（正则状态残留）；后台挂载时不再吞掉全应用 Ctrl+F；暂停时长不再计入翻译耗时；"已编辑"定位按钮修复（原必报错）；显示样式收敛为上下/左右对照两种；PDF 导出明确拆分双语对照/仅译文两项
- 内部：i18n 重复键块合并（EPUB 错误文案此前恒走英文兜底）、死状态/死键清理、document-parser 纯函数单测 19 例 + 设置迁移用例 5 例
- **界面整修**：图标统一 lucide（显示样式选择器/统计/已编辑徽章原为 emoji）；全部颜色改主题令牌——此前硬编码蓝色与状态色导致青绿主题、自定义强调色在文档页不生效，现三主题+任意强调色实拍验证全跟随；加密 PDF 密码弹窗补全样式（此前完全无样式、平铺在页面流中）；按钮补 focus 环与按压反馈、悬停操作按钮不再引起布局跳动、文档拖放区视觉升级

### 划词翻译（全面体检修复 59 项）
- **隐私（安全级）**：划词窗口自始不感知隐私模式——离线模式下选中文本仍走云端翻译源（免 key 的 Google 翻译直接穿透），无痕模式下译文照写磁盘缓存；现每次翻译前实时拉取隐私模式，无痕模式关闭缓存
- **剪贴板数据完整性（安全级）**：取词探测只备份纯文本——剪贴板里未粘贴的截图/文件/富文本会被探测的 Ctrl+C 覆盖后无法恢复而永久丢失；现全格式快照恢复，持有文件时拒绝探测；两套探测统一到单一互斥锁不再并发互踩；500ms 延迟恢复改即时，不再覆盖用户窗口期内的手动复制
- **承重墙修复**：焦点/插入符检测自第一天起失效（Win32 GetGUIThreadInfo 缺 koffi `_Inout_`，输出从不回写），三层探测退化为顶层类名白名单，WPF/Qt/Java 等应用划词静默失效；修复后焦点检测首次真正工作，控件类名单同步精确化，Word 走对剪贴板路径
- **钩子生命周期**：开关一次划词后双击翻译永久失效（uiohook 监听器累积互踩），现开关干净；EM_GETSEL 改超时调用，目标应用挂起不再冻结整个主进程
- **CapsLock 直出**：纯单击不再触发直出（避免终端里盲注 Ctrl+C 变 SIGINT 杀进程），终端类前台降级为点击确认；抓空不再静默——先出 loading，抓空翻红抖动可点重试，仍空引导截图翻译
- **多显示器**：副屏划词结果卡不再跳回主屏（几何按所在显示器工作区钳制）
- **文本处理**：俄语/阿拉伯语/泰语等非拉丁非 CJK 文字不再被"无有效文字"拒绝；含汉字的日文不再被误判为中文而翻错方向；历史条目补真实源/目标语言（此前语言元数据全错）
- **常驻窗口状态收口**：窗口复用（hide 不 close）导致上次会话状态泄漏——OCR 设置按钮阴魂、卡片跳旧位、隐藏后 TTS 继续朗读、翻译中来新触发被旧结果踩台，统一 resetSession + 世代守卫根治
- **冻结多窗**：窗口控制 IPC 改按调用方寻址，冻结卡操作不再误伤活动卡；冻结满 8 个改为拒绝而非静默关掉用户 pin 的窗
- **失败显性化**：钩子启动失败不再谎报绿色成功；「没有可用的翻译源」不再洗成「未知错误」；截图 loading 加超时兜底不再永久转圈；划词窗口语言随界面切换（不再需重启）
- **交互统一**：三路径结果卡统一自动隐藏（可配时长）+ 悬停暂停；置顶降 floating 级不再压制用户其他置顶工具；ESC 死监听与谎言文案删除（改右键/✕ 关闭）；emoji 图标换 lucide、触发点随主题色；CapsLock 警告改用 ConfirmDialog
- 内部：新增 utils/clipboard-capture 统一取词模块；死代码清扫约 600 行（划词 OCR 兜底整链、5 条死 IPC、native 死函数、幽灵设置键、零导入的 secure-storage 模块、screenshot.js 漂移死副本、强调色死脚手架）；FSM 新增手势门槛与看门狗续期单测

### 悬浮窗口（原玻璃窗，本版更名）
- 全面审计修复 21 项缺陷，重点：设置页五个开关此前保存即丢从未生效（配置桶改名只迁移了一半）、ESC/空格清空/右键清空三个交互自上线即失效（陈旧闭包）、译文含 % 使子窗格永久卡死、ESC/托盘关窗遗留置顶孤儿子窗格、隐私模式在悬浮窗内完全失效（离线模式下屏幕文本仍走云端，安全级）
- 捕获正确性：工具栏悬停不再使散点窗格集体位移 24px；连按空格不再把悬浮窗自己截进去；已分离子窗格不再被下次截图重复 OCR；混合 DPI 多屏的两处坐标错位修正；跨屏 0 尺寸裁剪兜底
- 透明度滑块修复（浅色主题 CSS 变量作用域陷阱 + 持久化 + 不再被设置广播重置）；移除四个无实现的设置开关（智能检测/刷新间隔/记住位置/自动置顶，置顶恒开）；移除无效毛玻璃与被裁阴影
- 代码层全量更名 glass → floating-window（文件/IPC 通道/组件/CSS/i18n），用户数据（窗口位置、本地设置、快捷键）启动时自动迁移
- 修复存量缺陷：secureStorage.isAvailable 通道名自始不匹配
- **标题栏拖动重写为手动实现**：CSS 拖动区（-webkit-app-region）在该透明无边框窗口上整体失效的存量问题（0.2.8 同样复现）；顺带修复分数缩放屏（如 175%）拖动时窗口逐帧膨胀（拖动起点锁定尺寸，DIP 舍入误差不再累积），窗口位置保存改 300ms 防抖不再拖动期间高频写盘

### 新增
- 本地 OCR 升级 PP-OCRv5：单模型覆盖简中/繁中/英/日，手写、竖排识别显著增强；引擎库由停更的 @gutenye/ocr-node 切换到 esearch-ocr（Apache-2.0，活跃维护）
- OCR 语言包按需下载：韩文 / 拉丁（法德西）/ 西里尔（俄文）/ 天城文（印地文）/ 阿拉伯字母 5 个语言包（各 ~8MB），设置页一键下载 / 更新 / 卸载，卸载即删整个包目录不留残留；清单与模型托管在 GitHub Release（tag `ocr-models`），日后更新模型只改 Release 资产、无需发新版
- Windows OCR 复活为正式引擎：系统自带零下载，作为本地兜底；本地模型缺失时自动降级并提示
- OCR 识别语言新增印地文、阿拉伯文

### 优化
- 本地 OCR 段落合并改用 esearch-ocr 排版分析（分栏 / 阅读方向感知），替代自研行合并
- 识别全程内存直通，不再写临时 PNG；主进程两条 OCR 调用路径（截图 / 划词）统一收敛到单引擎模块
- 引擎「修复」由运行时 npm 重装改为直接重新下载模型文件——不再要求用户机器装有 Node/npm

### 修复（首轮人工回归反馈）
- Windows OCR 识别路径自始即坏，两层根因都修了：①脚本调用了不存在的 `RandomAccessStream::FromStream`，按 WinRT 规范路径（StorageFile）重写；②脚本以内联 `-Command` 经 cmd.exe + PowerShell 双重解析传输，引号/换行被绞坏——改为 `-EncodedCommand`（base64）传输并抽成独立模块 electron/utils/windows-ocr.js（纯 Node 可测）。「自动检测」语言跟随系统语言包；中日文输出逐字空格清理（保留英文词间空格）
- 玻璃窗更改目标语言后，画面 / 文本未变化时不重新翻译（去重键未含目标语言）
- 划词卡片：源文本已是目标语言时原样直出 → 自动翻转 zh↔en，与玻璃窗行为一致
- 设置页 OCR 语言包列表窄窗下排版破裂（徽章换行压按钮）
- 「自动放大小图片」设置自始为纯 UI 摆设 → 在主进程引擎真实现（<1200px 截图放大识别、坐标换算回原图空间）

### 内部
- 删除运行时 npm install 引擎安装机制及全部相关 IPC / UI / 文案
- 删除 renderer 侧 text-merger 死代码（pipeline 仅 import 未调用；段落合并已由 esearch-ocr 排版分析接管）
- 基础模型经 scripts/fetch-ocr-models.js 拉取到 resources/ocr（gitignored），打包前执行；scripts/build-ocr-release.js 生成 ocr-models Release 资产（zips + manifest.json 含 sha256）
- 新增 electron/utils/ocr-engine.js（会话缓存 / 语言包解析）与 ocr-pack-manager.js（下载校验安装卸载）；ocr-packs 纯函数单测 13 例
- 依赖：+ esearch-ocr + @napi-rs/canvas + onnxruntime-node（转直接依赖）；- @gutenye/ocr-node（连带其内置 sharp）

## v0.2.7 — 2026-06-09

### 新增
- 隐私页数据管理面板：显示历史 / 收藏 / 缓存条数与设置、日志文件占用
- 文件日志落地（按天轮转、7 天清理、API 密钥自动脱敏），「打开日志目录」按钮可用
- 检测到翻译专用模型（MT）自动直出时，语气按钮旁显示「MT 模式」徽章
- 翻译风格（自然 / 精确 / 正式）选择重启后保持

### 优化
- 流式翻译性能包：逐 token 渲染改为按帧批量刷新（16/33ms 设备分档）、持久化写入节流、组件订阅精准化——长文流式的 CPU 与内存峰值显著下降，低端设备受益最明显
- 本地模型（LM Studio / Ollama）默认超时放宽到 180s，覆盖模型冷加载与思考型模型；超时报错会提示前往设置调整
- 启动时 API 密钥解密并行化，窗口冷启动更快
- 划词开启时，全局点击路径不再产生同步磁盘读
- 依赖安全更新：npm audit 漏洞 20 → 8，critical 清零
- 隐私页「使用统计」更名「本地使用统计」——程序不含任何遥测，统计仅保存在本机

### 修复
- 长文档段落译文被静默截断（移除 max_tokens 硬上限）
- LM Studio 切换模型后，相同原文命中旧模型的缓存译文
- 本地模型流式中断导致界面永久「翻译中」：新增空闲看门狗，任何输出（含思考增量）都会重置计时，思考型模型不会被误杀
- 主力翻译源非 LM Studio 时，风格改写 / AI 分析不可用
- 「清除缓存」实际未清空（内存缓存随后回写）

### 内部
- 注释统一英文并精简；新增错误文案 i18n 中英补齐
- 新增 stream-throttle 单元测试；vite optimizeDeps 补齐 dayjs 深路径（修开发模式懒加载面板白屏）

## v0.2.5 / v0.2.6 — 2026-05-11 / 2026-05-26

详见 [GitHub Releases](https://github.com/Tianao0110/T-Translate/releases) 与 git log（v0.2.5：划词三层探测修复与测试基建；v0.2.6：OpenAI-compatible presets 架构合并 4 个 LLM provider）。

## v0.2.4 — 2026-04-24

### 新增
- 划词翻译新增 CapsLock sticky 直出模式（设置页可独立开关）

### 优化
- 剪贴板轮询 500ms → 800ms（轮询次数 10 → 16），降低「按下没内容」发生率
- OCR 兜底阈值放宽（外层 10×5 → 8×4，内层 20×10 → 12×6），小区域文本更易识别

### 修复
- 划词直出窗口初始尺寸异常
- FSM 新增 Condition D「快速选词」，修自动检测路径飞快划词偶尔漏判

### 内部
- 清理死代码（移除 getWindowInfoAtPoint 残留 import 等）
- 新增 Chrome Alt 可行性 spike 脚本 + TODOS.md（追踪 v0.3 候选）
- tests/setup.js 预埋（完整 test toolchain 延后独立 PR 处理）

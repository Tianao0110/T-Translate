# T-Translate 使用说明

本说明书随程序分发，在「设置 → 使用说明」里可以离线查看；GitHub 上的同一份文件内容相同。

## 0. 开始使用

### 0.1 安装与首次启动

- 双击安装包安装，可以自己选安装位置。
- 卸载不会删掉你的设置、历史记录和下载好的模型，重新安装后还在。
- 安装后，右键 .pdf、.docx、.txt 文件会多出一项「用 T-Translate 翻译」，点它直接开始翻译这个文件。
- 第一次打开会看到一页功能介绍，点「开始使用」进入主窗口。
- 装好就能用 Google 翻译，需要联网。想不联网也能翻译，把内置模型的文件放进模型文件夹（见第 5 章），或者在本机运行 LM Studio 或 Ollama。翻译面板上方出现「还没有可用的翻译源」时，点「去设置」。
- 设置页默认只显示常用项，点左下角的「完整」可以看到全部设置。
- 点主窗口右上角的关闭，程序会缩到托盘继续运行。要彻底退出，右键托盘图标选「退出」。

### 0.2 托盘图标

- 单击：开关划词翻译。开着时，鼠标放到图标上的提示里带一个 ✓。
- 双击：打开主窗口。
- 右键菜单：截图翻译、悬浮窗口、划词翻译、设置、退出。
- 想让程序开机自动运行：设置 → 界面外观 → 启动，打开「开机自启动」。再打开「启动后自动开启划词翻译」，开机后不用碰托盘，直接就能划词。

### 0.3 全局快捷键

这五个快捷键在任何程序里都能按：

- **Alt+Q**：截图翻译
- **Ctrl+Shift+W**：显示 / 隐藏主窗口
- **Ctrl+Alt+G**：打开 / 关闭悬浮窗口
- **Ctrl+Shift+T**：开关划词翻译
- **Ctrl+Alt+Space**：让悬浮窗口再截译一次

都可以在 设置 → 界面外观 → 快捷键 里改。如果启动时提示某个快捷键被别的程序占用了，换一个组合就好。全部快捷键见第 6 章。

### 0.4 三种隐私模式

在 设置 → 隐私模式 里切换：

- **标准模式**：功能全开，自动保存历史记录。
- **无痕模式**：什么都不保存，关掉窗口就清空。
- **离线模式**：完全不联网，只用本机的翻译源和识别引擎。

想看每个功能在三种模式下的差别，点隐私模式页里的「详细」。

## 1. 主窗口

### 1.1 翻译面板

（待写）

### 1.2 历史记录

（待写）

### 1.3 收藏

（待写）

### 1.4 文档翻译

（待写）

### 1.5 AI 动作

（待写）

### 1.6 标题栏与窗口行为

（待写）

## 2. 划词翻译

（待写）

## 3. 悬浮窗

### 3.1 玻璃、置顶、散点与整段

（待写）

### 3.2 截图识别

（待写）

### 3.3 听译实时字幕

（待写）

## 4. 设置页

（待写）

## 5. 模型与下载

（待写）

## 6. 快捷键总表

### 6.1 全局快捷键

在任何程序里都能按，可以在 设置 → 界面外观 → 快捷键 里改：

- **Alt+Q**：截图翻译
- **Ctrl+Shift+W**：显示 / 隐藏主窗口
- **Ctrl+Alt+G**：打开 / 关闭悬浮窗口
- **Ctrl+Shift+T**：开关划词翻译
- **Ctrl+Alt+Space**：让悬浮窗口再截译一次

### 6.2 主窗口

- **Ctrl+1 到 Ctrl+5**：切换到翻译、历史、收藏、设置、文档页
- **Ctrl+Enter**：翻译原文框里的文字
- **Ctrl+F**：在历史、收藏、文档页里搜索
- **Ctrl+,**：打开设置
- **F11**：全屏 / 退出全屏
- **Ctrl+Q**：退出程序

### 6.3 悬浮窗口

- **空格**：截译一次；已经有结果时按一下清空
- **Esc**：关闭窗口。正在穿透就先退出穿透，开着记录面板就先关面板，有散点面板就先清面板
- **按住 Alt**：暂时穿透，鼠标点到窗口下面的程序上，松开恢复
- **Ctrl+H**：打开 / 关闭最近记录
- **双击某个散点面板**：把它变成独立的小窗口

### 6.4 划词翻译

- **CapsLock**：在 设置 → 划词翻译 里打开「CapsLock 直出模式」后，CapsLock 灯亮着时划词直接弹出翻译，不用再点小图标。灯灭就回到平时的流程。

## 7. 数据放在哪里与备份

（待写）

## 8. 常见问题

（待写）
